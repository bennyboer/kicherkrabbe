dependencies {
    implementation project(':libs:permissions:permissions-starter')

    testImplementation project(':libs:persistence:persistence-testing')
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}