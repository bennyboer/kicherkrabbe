plugins {
    id 'kicherkrabbe.library-conventions'
}

dependencies {
    implementation project(':libs:changes:changes-starter')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}
